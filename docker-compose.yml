---
version: "3.3"
services:
  db:
    image: postgres:12
    container_name: "postgres"
    env_file:
      - .database/database.env
    volumes:
      - database-data:/var/lib/postgresql/data
    ports:
      - 5432:5432

volumes:
  database-data:
